<h1>Boards</h1>


<%= @board.name %><br/>

<%= link_to "Delete #{@board.name}?", @board, method: :delete%>
<%= link_to "Update #{@board.name}?", edit_board_path(@board)%>
<%= link_to "New List", new_board_list_path(@board)%>

<% @board.lists.each do |list|%>
  <p><%= list.name %></p>
  <%= link_to "Add Task", new_list_task_path(list)%>
<% end %>